# 78. Subsets

对于每个数字而言，我们都可以放或不放, 时间复杂度是 O(2^N), given N numbers, at most have 2^N different subsets

如果一个数字已经用过了，那我们要从这个数字之后开始选择，这样就不会有重复的子集了

example [1,2,3]

() 中代表之后可选的区间

[]  -> [1] (2,3) -> [1,2] (2) -> [1,2,3]
                 -> [1,3]
    -> [2] (3)  -> [2,3]
    -> [3]


